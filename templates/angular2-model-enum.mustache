export type {{ name }} =
{{#enumParameters}}
    "{{name}}"{{^last}} |{{/last}}{{#last}};{{/last}}
{{/enumParameters}}


name.prototype.getValidProperties = function(): string[] {

    return [{{#enumParameters}}
        "{{name}}"{{^last}},{{/last}}{{/enumParameters}}
    ];
}